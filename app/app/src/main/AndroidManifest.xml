<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cmsc355.ccnd.gymrat">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeScreen" />
        <activity
            android:name=".CustomWorkout"
            android:exported="true" />
        <activity android:name=".CustomWorkoutExercises" />
        <activity android:name=".LiftingTips" />
        <activity
            android:name=".TipsForExercise"
            android:label="@string/title_activity_tips_for_exercise"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MyProfile" />
        <activity android:name=".AddWorkout" />
        <activity android:name=".ExampleWorkout" />
        <activity android:name=".DisplayExampleWorkout" />
        <activity android:name=".CaloriesCounter" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".GetUserInfo" />
        <activity android:name=".WeightTracker" />
        <activity android:name=".MyWorkouts" />
        <activity android:name=".SetGoals" />
        <activity
            android:name=".WeightTrackerDisplay"
            android:label="@string/title_activity_weight_tracker_display"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DisplayWorkouts"
            android:label="@string/title_activity_display_workouts"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>